Simple London Tube app that uses MVI and MVP architecture
=========================================================

## To build the project
`./gradlew assembleDebug`

## To run the tests
`./gradlew test`

## To run Detekt static analysis tool
`./gradlew detect --continue`